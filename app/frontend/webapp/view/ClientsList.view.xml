<mvc:View controllerName="bksoft.frontend.controller.ClientsList" xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc">
    <Page showNavButton="true" navButtonPress=".onNavBack">
        <FlexBox alignItems="Center" justifyContent="Center">
            <List headerText="{i18n>InputHeader}" width="40em">
                <InputListItem label="{i18n>eFN}">
                    <Input value="{/client/firstName}" width="20em" />
                </InputListItem>
                <InputListItem label="{i18n>eSN}">
                    <Input value="{/client/secondName}" width="20em" />
                </InputListItem>
                <InputListItem label="{i18n>ePhNum}">
                    <Input value="{/client/phoneNum}" type="Number" width="20em" />
                </InputListItem>
                <InputListItem label="{i18n>eEmail}">
                    <Input value="{/client/email}" width="20em" />
                </InputListItem>

            </List>
        </FlexBox>
        <FlexBox alignItems="Center" justifyContent="Center">
            <Button text="Submit" press=".onSubmit" margin="30px" />
        </FlexBox>
        <FlexBox alignItems="Center" justifyContent="Center">
            <Table id="table" width="auto" items="{
                    path: 'clientsList>/value'
                }" growing="true" growingScrollToLoad="true" updateFinished=".onUpdateFinished">
                <headerToolbar>
                    <OverflowToolbar>
                        <ToolbarSpacer />
                        <SearchField id="searchField" search=".onSearch">
                            <layoutData>
                                <OverflowToolbarLayoutData maxWidth="200px" priority="NeverOverflow" />
                            </layoutData>
                        </SearchField>
                    </OverflowToolbar>
                </headerToolbar>

                <columns>
                    <Column id="FnameColumn" />
                    <Column id="SnameColumn" />
                    <Column id="phoneColumn" />
                    <Column id="emailColumn" />
                </columns>

                <items>
                    <ColumnListItem>
                        <cells>
                            <ObjectIdentifier title="{clientsList>FirstName}" />
                            <ObjectIdentifier title="{clientsList>SecondName}" />
                            <ObjectIdentifier title="{clientsList>PhoneNumber}" />
                            <ObjectIdentifier title="{clientsList>email}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </FlexBox>
    </Page>
</mvc:View>